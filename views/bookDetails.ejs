<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= book.title %> - Full Review</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #f7f7f7;
            padding: 40px 20px;
        }
        
        .container {
            max-width: 800px;
            margin: auto;
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        
        h1 {
            font-size: 32px;
            margin-bottom: 10px;
        }
        
        .cover {
            width: 200px;
            height: auto;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        
        .book-info {
            display: flex;
            align-items: flex-start;
            gap: 30px;
            margin-bottom: 30px;
            flex-wrap: wrap;
            /* Allows stacking when needed */
        }
        
        .cover {
            width: 180px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            flex-shrink: 0;
        }
        
        .meta {
            flex: 1;
            min-width: 250px;
        }
        
        .meta h1 {
            margin-top: 0;
            font-size: 28px;
        }
        
        .meta {
            font-size: 16px;
            color: #555;
            margin-bottom: 20px;
        }
        /* Mobile responsiveness */
        
        @media (max-width: 600px) {
            .book-info {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }
            .cover {
                margin-bottom: 20px;
            }
            .meta {
                text-align: left;
                width: 100%;
            }
        }
        
        .review-text {
            font-size: 18px;
            line-height: 1.8;
            text-align: justify;
            margin-bottom: 30px;
        }
        
        .buttons {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }
        
        .buttons a,
        .buttons form button {
            padding: 10px 20px;
            border: none;
            border-radius: 25px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            text-decoration: none;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s ease;
        }
        
        .btn-back {
            background-color: #ffcc00;
            color: black;
        }
        
        .btn-back:hover {
            background-color: #e6b800;
        }
        
        .btn-edit {
            background-color: #dedede;
            color: black;
        }
        
        .btn-edit:hover {
            background-color: #0056b3;
        }
        
        .btn-delete {
            background-color: #dedede;
            color: black;
        }
        
        .btn-delete:hover {
            background-color: #c82333;
        }
        
        html,
        body {
            height: 100%;
            margin: 0;
        }
        
        .main-wrapper {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="book-info"> <img class="cover" src="<%= book.cover_id.startsWith('/uploads/') ? book.cover_id : 'https://covers.openlibrary.org/b/id/' + book.cover_id + '-M.jpg' %>" alt="Book Cover">
            <div class="meta">
                <h1><%= book.title %></h1>
                <p><strong>Author:</strong>
                    <%= book.author %>
                </p>
                <p><strong>Category:</strong>
                    <%= book.category || 'N/A' %>
                </p>
                <p><strong>Rating:</strong>
                    <%= book.rating %>/10</p>
                <p><strong>Date Read:</strong>
                    <%= new Date(book.date_read).toDateString() %>
                </p>
            </div>
        </div>
        <div class="review-text"> <strong>Review:</strong>
            <br>
            <%= book.notes %>
        </div>
        <div class="buttons"> <a href="/" class="btn-back">⬅️ Back to Home</a> <a href="/edit/<%= book.id %>" class="btn-edit">✏️ Edit</a>
            <form action="/delete/<%= book.id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this book?');">
                <button type="submit" class="btn-delete">🗑️ Delete</button>
            </form>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>

</html>